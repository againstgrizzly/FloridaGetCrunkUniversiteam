import java.awt.event.KeyEvent;

import java.awt.event.KeyListener;

import javax.swing.JTextField;

public class Text extends Window implements KeyListener{
	
	public static String Temp = "";
	

	Text(){
		
		textBox.setText("23.0"); // set this way because that is room tempture
	
	}

	@Override
	public void keyPressed(KeyEvent arg0) {}

	@Override
	public void keyReleased(KeyEvent e) {
		
		char keyPressed = e.getKeyChar(); //changes the event to a char that I can test
		
		if(keyPressed >= '0'  && keyPressed <= '9'){ //testing the char to see if it was a vaild input
			
			Temp += keyPressed; // this temperally holds the value that has been put into the text field
			
			tempreture = Float.parseFloat(Temp); //simply parseing the string value into a workable float number


		}else{
			
			textBox.setText(textBox.getText().substring(0,textBox.getText().length() - 1));

			//This deletes the last input from the keybord if it was invaild
		}
		
	}

	@Override
	public void keyTyped(KeyEvent arg0) {}

}